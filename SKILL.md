---
name: ai-video-scene-builder
description: 将中文 AI 视频想法、场景或戏剧片段拆成可执行的场景包，适用于 AI 短剧、漫剧、广告、对话戏、单场景戏和教学型场景拆解。
---

# AI Video Scene Builder

把一个中文 AI 视频想法，从模糊概念推进成可以真正落地执行的场景包。

适用任务：
- 一句话视频想法
- 一段剧情或一个戏剧片段
- 单场景短剧 / 广告 / 情绪短片
- AI 视频教学案例拆解
- 需要后续继续转成平台提示词的分镜方案

## 核心输出顺序

默认按这个顺序输出：

1. 一句话场景定义
2. 场景核心判断
3. 情绪线
4. 时长建议与节奏
5. 声音方案
6. 分镜脚本表
7. 素材准备清单
8. 镜头提示词
9. 可选图表建议

如果用户明确要 `Seedance`：

1. 先完成正常中文场景包
2. 再把分镜表转成按组的 Seedance 片段提示词
3. 上游分镜保持中文、可执行、可教学
4. 下游 Seedance 输出也默认用中文，除非用户明确要求英文
5. 每段 Seedance 提示词默认不超过 `15秒`

## 工作流

1. 先判断这是“场景级任务”还是“项目级任务”。
2. 如果输入很模糊，先压缩成一句话场景定义。
3. 先判断这场戏真正讲的是什么，再展开镜头。
4. 先定节奏和时长，再定画面。
5. 先设计声音，再设计镜头。
6. 先保证连续性，再考虑“好看”。
7. 先产出分镜脚本表，再继续写提示词。
8. 如有需要，再转成教学拆解或图表。

## 张力判断步骤

在“判断这场戏真正讲什么”之后、“展开镜头”之前，默认先补做一轮 `张力判断`。

至少先明确这 4 件事：

1. 这场戏的主爆点是什么，最多 `1-2个`
2. 最晚第几秒必须出现第一次有效钩子
3. 哪些镜头属于 `铺垫镜头`、哪些属于 `推进镜头`、哪些必须升级成 `攻击镜头`
4. 集尾 / 段落尾是 `信息收束` 还是 `追更钩子`

默认目标：

- 不让整场戏所有镜头平均用力
- 不让前段只是在解释信息，没有抓人的钩子
- 不让爆点只有台词，没有反应、停顿、压迫和声音反差

## 镜头功能分层

短剧、剧情向视频、冲突戏，默认把镜头分成三类来组织：

### 1. 铺垫镜头

- 用于交代人物、空间、关系、基础状态
- 可以稳，但不能连着太久
- 默认连续不超过 `3-4个`

### 2. 推进镜头

- 用于改变关系、推进动作、暴露新信息
- 镜头结束时，人物状态或关系状态默认要发生变化
- 如果镜头结束前后什么都没变，优先怀疑它写得太平

### 3. 攻击镜头

- 专门负责打击观众情绪
- 常用于：羞辱、发现真相、崩塌、反转、对峙、钩子、觉醒、追悔
- 优先使用：近景、特写、反应镜头、主观感受、停顿、遮挡压迫、声音反差、可视动作锚点

硬规则：

- 单集短剧里，每 `15-25秒` 至少要有一个 `攻击镜头`
- 爆点段不能只有说台词的镜头，必须补 `反应 / 停顿 / 压迫 / 声音设计`
- 集尾默认落在 `动作 / 反应 / 钩子`，而不是落在解释

## 张力增强层

如果用户要的是短剧分镜、剧情分镜、爽点分镜、视频张力更强的版本，默认可以在稳定分镜表后追加一层 `张力增强层`。

交付方式：

1. 先给稳定版分镜脚本表
2. 再给一段简短的 `张力增强层`
3. 张力增强层只改关键镜头，不重写全表
4. 每场戏增强建议默认控制在 `5-8条`

增强层优先从这些维度里选：

- `钩子提前`：把第一次有效抓人镜头前移，缩短纯铺垫
- `反应放大`：把受伤者、旁观者、加害者的反应写得更狠
- `节奏切换`：爆点前后切镜速度改变，制造停顿或骤快
- `调度压迫`：前后景、遮挡、逼近、退让、高低位更明确
- `声音反差`：喧闹突停、耳鸣、呼吸、门外一句话、空拍
- `动作锚点`：解发、松手、让位、关门、抬眼、转身、撕纸、摔杯

默认原则：

- 先有稳定叙事，再加张力
- 不要把所有镜头都写成“攻击镜头”
- 张力增强的目标是更抓人，不是更浮夸

## 短剧单集默认规则

当用户要拆的是“短剧某一集 / 连续剧单集 / 一集完整故事性分镜”，默认按 `3分钟左右短剧完整叙事版` 处理，而不是先按预告混剪版处理。

默认判断：

- 如果用户说的是“第X集”“一集”“单集”“按集输出”“完整故事性”“短平快节奏”，优先视为 `单集完整叙事任务`
- 如果用户没特别说明，但给的是短剧剧本中的单集内容，也优先按 `单集完整叙事任务`
- 只有用户明确说“预告版”“混剪版”“高光版”“招商版”“先出核心镜头”，才压缩成高浓度概览版

单集完整叙事任务默认目标：

- 单集时长默认按 `约3分钟` 估算，可在 `2.5-4分钟` 内微调
- 默认先保证“起因-推进-爆点-收尾钩子”完整成立
- 默认比预告版镜头更密，通常会包含 `32-45个镜头`，也可以先交付成 `15-25个场景` 再细化
- 默认节奏是短平快，但不能为了快把人物动机和情绪断裂拍没
- 每集结尾默认要留追更钩子或下一集承接点
- 默认同时检查 `铺垫镜头 / 推进镜头 / 攻击镜头` 的比例，不要全程平均用力
- 默认前 `20-30秒` 内尽量出现第一处有效钩子

当采用这个模式时，优先输出顺序：

1. 集数定位
2. 节奏判断
3. 完整分镜脚本表
4. 结构说明
5. 执行建议

如果用户还给了某套“参考规则 / 样例资料 / 别的项目格式”，先对齐那个交付格式，再保留上面的完整叙事原则。

## Seedance 下游流程

当用户明确要 `Seedance` 提示词时，按这条顺序：

1. 输出完整中文分镜脚本表
2. 把镜头按 `3-6秒` 左右分组；如需更长，也必须控制在 `15秒` 以内
3. 进行分镜到 Seedance 的映射：
   - `景别 + 机位` -> 镜头类型 + 运镜表达
   - `画面内容` -> 主体 + 可见调度
   - `动作` -> 主动作节拍
   - `情绪目的` -> 节奏密度、风格收放
   - `声音提示` -> 声音层提示
   - `时长` -> 片段长度与时间顺序
4. 最终输出：
   - 分镜脚本表
   - Seedance 分组提示词
   - 连续性风险提醒

## Seedance 中文输出策略

当用户明确要 `Seedance` 时，不再引用 `seedance-2.0` 的规则底座，只参考：

- `.claude/skills/seedance`
  - 负责中文输出层
  - 参考它的中文措辞、模板组织和场景分类方式

默认原则：

1. 先用中文分镜把场景拆清楚
2. 再把分镜表按组转成 Seedance 片段提示词
3. 中文成稿优先参考 `.claude/skills/seedance` 的表达方式
4. 输出格式尽量贴近该 skill 的中文实战写法
5. 保留 `ai-video-scene-builder` 自己的分镜、调度、连续性、特效细化规则
6. 每组提示词默认控制在 `15秒` 以内，优先推荐 `4-8秒`

## 强规则

- 不要一上来就写提示词
- 不要先堆“漂亮镜头”
- 不要用空泛概念代替执行结构
- 优先连续性，其次才是花样
- 先解决“这场戏在讲什么”，再解决“怎么生成”
- 默认输出标准分镜脚本表，除非用户明确说只要粗版
- 短剧单集默认先保证完整故事性，再考虑混剪感和炫技感
- 短剧单集默认不要先压成十几个高光镜头，除非用户明确要预告/混剪版
- 默认不要让整场戏只剩平均叙述，没有攻击镜头
- 默认不要把爆点只写成台词内容，要补反应、停顿、空间压迫或声音反差
- 提示词不要混进分镜表
- 分镜表默认用中文输出
- Seedance 提示词默认也用中文输出
- Seedance 分组提示词每段默认不得超过 `15秒`
- 如果用户提供了现成剧本，默认不得擅自改写原台词
- 分镜表里如需引用台词，默认保持原句，不做润色、不做压缩、不做情绪化改写
- 除非用户明确要求“润色台词 / 重写台词 / 改对白 / 优化对白”，否则只允许补镜头、动作、反应、调度，不改台词内容
- 输出内容默认视为可直接使用的分镜文档或教学文档，不写成对用户的回复
- 不要出现 `如果你要拿它去教学`、`你给的参考图`、`下面我来拆`、`我建议这样写`、`我帮你补一版` 这类对话痕迹
- 引用用户提供的截图、片段、样例时，统一改写为 `参考片段`、`参考案例`、`该场景` 这类中性表述

## 分镜脚本表固定格式

默认使用这个字段顺序：

| 镜头号 | 时长 | 景别 | 机位 | 画面内容 | 动作 | 情绪目的 | 声音提示 | 全局提示词 | 细节增强提示词 |
|---|---:|---|---|---|---|---|---|---|---|
| 1 | 3.6秒 | 远景 | 平视 | ... | ... | ... | ... | ... | ... |

使用原则：

- 前 8 列负责分镜结构
- `全局提示词` 负责整场戏统一的审美底座和技术质感
- `细节增强提示词` 负责单镜头的局部质感、材质、光影和完成度
- 默认直接按完整 10 列输出，减少后续再补字段的返工
- 如果用户明确只要轻量版，才允许临时压缩为前 8 列

## `画面内容` 的写法标准

`画面内容` 不能只写成一句笼统摘要。  
它要足够支撑：
- 分镜阅读
- 出关键帧
- 写视频提示词
- 检查连续性
- 课堂讲解

默认尽量写清：
- 谁在画面里
- 人物和环境的相对位置
- 人物初始出场方位
- 人物当前站位或空域层级
- 人物彼此的前后 / 左右 / 高低关系
- 谁在前景、谁在中景、谁在后景
- 动作从哪个方向进入画面、往哪个方向离开
- 镜头先看到什么
- 画面里正在发生什么
- 命中 / 碰撞 / 反应 / 失衡 / 环境反馈是否可见
- 前景 / 中景 / 后景里什么最重要

当用户要的是更像影视画面的版本，`画面内容` 默认允许再补 4 类信息：

- 光线关系
- 冷暖关系
- 构图重心
- 空间压迫感

也就是说，`画面内容` 不只是在报信息，还要说明这张画面为什么成立。

优先鼓励补进这些经典画面表达：

- `冷暖对比`
- `明暗反差`
- `视觉中心 / 视觉重心`
- `前景遮挡`
- `框中框`
- `空间压缩`
- `高低位关系`
- `前后景层次`

弱写法：
- `山路上有一辆马车在跑`

更好的写法：
- `冷月夜色压住整条官路，马车上的一点暖灯火落在画面中部成为视觉中心，两侧林影把空间压得更深更冷`

弱写法：
- `两个人在空中打斗`

更好的写法：
- `少女从画面左下方向上反冲，男主在右后高位回身拦截，蓝色与暗色推进尾迹在两人之间交叉，下方可见海面和高楼落差`

如果场景里有两个及以上角色，默认优先补齐这些硬信息：
- 谁从画面左 / 右 / 上 / 下哪个方向入镜
- 谁占左前 / 右后 / 高位 / 低位
- 谁更靠近镜头
- 谁挡住谁，谁压住谁的路线
- 镜头结束时双方相对位置有没有变化

## 特效镜头写法标准

如果镜头里有粒子、烟雾、火花、尾焰、冲击波、装甲展开、能量刃、碎片等可见特效，不要只写：
- `特效爆发`
- `粒子飞散`
- `烟雾弥漫`
- `能量出现`

默认按这个结构写：

`动作主体 + 特效来源 + 特效形态 + 运动方式 + 落点/受力点 + 环境反馈 + 镜头内可见重点`

尽量写清：
- 特效从哪里出现
- 是什么形态
- 怎么运动
- 打到哪里
- 打中后画面里能看到什么
- 对空气、烟雾、地面、建筑、装甲造成什么反馈

## 动作戏额外规则

打斗 / 追逐 / 空战 / 机甲 / 奇幻 / 科幻镜头里，`画面内容` 还要尽量说明：
- 双方相对位置
- 初始出场方位
- 前后景层级
- 空间高度关系
- 动作方向
- 谁先出手
- 力量打在哪里
- 谁失衡 / 谁回控 / 谁反击
- 环境只是背景，还是危险的一部分

默认避免这类容易“乱飘”的写法：
- `两人在空中缠斗`
- `角色高速交手`
- `一前一后追逐`

优先改成：
- `少女从画面左前低位斜向上切入，男主从右后高位俯冲压下`
- `男主在前景右侧出拳，少女在中景左侧后仰闪避`
- `镜头开始时男主占高位，镜头结束时少女绕到他左后侧`

## 时间精度规则

`时长` 默认支持 `0.1秒` 粒度。

优先写：
- `2.4秒`
- `3.2秒`
- `0.8秒`

不要默认全部四舍五入成整数秒。

一位小数主要用于：
- 打斗
- 追逐
- 空战
- 特效爆发
- 反应镜头
- 转身 / 停顿 / 回头
- 受击 / 失衡 / 回控 / 翻盘
- 广告型强节奏镜头

常见时长区间：
- 建立镜头：`2.5 - 4.5秒`
- 中景推进：`2.0 - 3.5秒`
- 近景情绪反应：`1.2 - 2.8秒`
- 特写动作细节：`0.8 - 2.0秒`
- 动作爆发 / 对撞 / 闪避：`0.6 - 1.8秒`
- 转身 / 回头 / 停顿：`1.0 - 2.5秒`

## 情感短剧高张力规则

羞辱、分手、偷听、误解、发现真相、追悔、对峙这类情感短剧，默认在连续性之外，再补一层高张力判断。

### 羞辱戏

默认优先拍出三段打击：

1. `环境失衡`：热闹、起哄、旁人看见、空气变尴尬
2. `关系失衡`：让位、偏爱、站位压制、身体退让
3. `主观受伤`：停住、耳鸣、眼神空掉、手指发紧、动作僵住

### 偷听 / 发现真相戏

默认优先考虑：

- 声音先到，理解后到
- 或画面先到，反应后炸
- 不要一上来就把真相讲完，先给停顿和吸收过程
- 如果真相通过对白暴露，默认引用剧本原句，不改对白本身，只增强前后反应和声音设计

### 对峙戏

默认补清楚这些压制关系：

- 谁逼近
- 谁后退
- 谁占高位 / 低位
- 谁更靠近镜头
- 谁被卡到边缘
- 镜头结束时双方位置有没有变化

### 断裂 / 觉醒戏

默认尽量给一个清晰的可视化动作锚点：

- 解发
- 松手
- 转身
- 让位
- 关门
- 撕纸
- 抬眼
- 摔杯

### 集尾规则

情感短剧的集尾默认优先收在：

- 人物即将做什么
- 人物刚做出了一个不可逆动作
- 一句钩子台词落在动作之后

默认不要收在“说明完了”。

## 台词处理规则

如果输入里已经有剧本台词、对白稿、逐句文本，默认把这些内容视为上游定稿文本。

默认做法：

- 可以决定哪句台词放在哪个镜头里
- 可以决定哪句台词前后补什么动作、停顿、反应、运镜、声音
- 可以决定哪句台词作为钩子句、爆点句、收尾句
- 可以在分镜表里只引用关键台词，而不是把所有台词都抄进表格

默认禁止：

- 擅自润色原台词
- 擅自缩写、改写、换词、重组语序
- 把原本平静的台词改成更抓马的台词
- 把原本口语化的台词改成更“像短剧”的文案腔

只有在用户明确要求以下意图时，才可以动台词：

- `润色台词`
- `改对白`
- `重写台词`
- `增强台词爽感`
- `优化对白节奏`

如果用户既要分镜又要更强张力，默认优先增强：

- 镜头调度
- 人物反应
- 空间压迫
- 声音反差
- 节奏切换

而不是先改台词。

## 全局提示词与细节增强提示词

当用户希望分镜继续服务生图、图像资产或视频提示词时，默认允许在分镜表中加入两类提示词列。

### 1. 全局提示词

作用：

- 锁定整场戏的技术质感
- 锁定整场戏的风格方向
- 避免后续每张图像不同项目

写法要求：

- 更像整场戏共用的底层风格词
- 不要每镜乱变
- 通常控制在 `8-15个` 关键词或短语内

命名原则：

- 输出字段统一写成 `全局提示词`
- 这一列实际承担的是“全局风格统一”作用
- 不再和 `全局风格提示词` 混用，避免表头出现两套名字

常见内容：

- 风格类型：`国风CG动画` / `高精度CG动画`
- 技术质感：`电影级光影` / `虚幻引擎5质感`
- 画面方向：`电影感构图` / `写实材质`
- 氛围方向：`冷月夜色` / `体积雾` / `冷暖对比`

### 2. 细节增强提示词

作用：

- 把单镜头做得更厚
- 明确材质、局部光影、环境颗粒和局部动作细节

写法要求：

- 随镜头变化
- 更偏局部材质、光影、环境、动态细节
- 通常控制在 `4-10个` 关键词或短语内

常见内容：

- `金属刀锋反光`
- `布帘摆动`
- `发丝与衣袍受风`
- `湿土与碎石细节`
- `马车木质纹理`
- `体积光`
- `轮廓光`
- `景深虚化`
- `前景遮挡`
- `高反差明暗`

## 输出模式

- 完整场景包
- 短剧单集完整叙事版
- 分镜脚本表 + 张力增强层
- 只要情绪线
- 只要声音方案
- 只要分镜脚本表
- 只要素材清单
- 只要镜头提示词
- Seedance 分组提示词
- 分镜 + Seedance 联用输出
- 教学拆解
- Mermaid / Excalidraw 图表骨架

## 需要时再读的参考

- `references/workflow.md`
- `references/scene-structure.md`
- `references/shot-language.md`
- `references/continuity-rules.md`
- `references/sound-design.md`
- `references/prompt-patterns.md`
- `references/output-patterns.md`
- `references/seedance-integration.md`

## 可复用模板

- `assets/storyboard-template.md`
- `assets/sound-script-template.md`
- `assets/material-checklist-template.md`
- `assets/shot-prompt-template.md`
- `assets/seedance-clip-template.md`
- `assets/scene-diagram-template.md`

## 语气规则

- 默认用自然中文
- 解释判断，不只报结果
- 避免教材腔和产品腔
- 如果是教学场景，要说明为什么这场戏成立
- 默认去除聊天语气、交付说明语气和“我/你”对话痕迹，正文直接进入可授课或可执行表述

## 默认交付形态

除非用户明确说只要一部分，否则默认完整输出：

1. 一句话场景定义
2. 场景核心判断
3. 情绪线
4. 时长建议
5. 声音方案
6. 分镜脚本表
7. 素材准备清单
8. 镜头提示词
9. 可选图表建议

如果用户要的是短剧单集、剧情冲突、爽点分镜，或者明确说“视频张力不够 / 想更抓人 / 想更有戏”，优先默认输出：

1. 集数定位 / 一句话场景定义
2. 节奏判断
3. 分镜脚本表
4. 张力增强层
5. 执行建议

如果用户明确要 `Seedance`，默认完整输出：

1. 一句话场景定义
2. 场景核心判断
3. 分镜脚本表
4. Seedance 分组提示词
5. 连续性风险提醒
6. 可选补充：哪些镜头适合 I2V / V2V / R2V

## 反平铺检查清单

出稿前默认快速检查：

- 前 `20-30秒` 里是否已经出现第一处有效钩子
- 是否有 `3个以上` 连续镜头只在重复同一种信息
- 爆点镜头是否只有台词，没有停顿、反应、压迫或声音设计
- 全集 / 全段是否至少有 `3个` 可直接做封面帧或切片的镜头
- 集尾是否有明确的下一步承接力

如果这些问题里有两个及以上答不上来，默认说明这版分镜仍然偏平，需要继续加强张力层。
