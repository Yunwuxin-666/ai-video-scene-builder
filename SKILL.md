---
name: AIvideo-prompt
description: 将中文 AI 视频想法、场景或戏剧片段拆成可执行的场景包，默认采用成人科幻动画短篇的导演思维：高概念设定、黑色幽默、文明讽刺、类型片升级、强视听反差、声音叙事和反转收束。适用于 AI 短剧、漫剧、科幻寓言、科技失控、微型文明、末日喜剧、怪物寓言、广告、对话戏、单场景戏和教学型场景拆解。
---

# AIvideo-prompt

把一个中文 AI 视频想法，从模糊概念推进成可以真正落地执行的场景包。

默认输出风格：成人科幻动画短篇导演思维。优先把日常入口、小空间或普通物件升级成高概念短片，用黑色幽默、文明讽刺、科技异化、后人类观察、微型文明、怪物寓言、反战寓言、系统失控等方式组织剧作和分镜。

默认不要复制任何具体作品、单集、角色或专有名词。只吸收通用方法：高概念压缩、类型片升级、强视觉母题、声音反差、视角反转、黑色或温柔的结尾余味。

适用任务：
- 一句话视频想法
- 一段剧情或一个戏剧片段
- 单场景短剧 / 广告 / 情绪短片
- 成人科幻动画短篇 / 黑色幽默科幻 / 科技讽刺短片
- 微型文明、机器人观察、人类废墟、智能系统失控、怪物寓言、末日喜剧
- AI 视频教学案例拆解
- 需要后续继续转成平台提示词的分镜方案

## 默认风格引擎

除非用户明确指定其他风格，默认先按这个公式生成或整理场景：

`日常空间/物件 + 极端科幻异常 + 人类弱点讽刺 + 类型片升级 + 声音反差 + 结尾反转`

优先选择这些短篇模式：

- `后人类观察喜剧`：非人类角色像游客、考古者或客服一样观察人类遗迹和荒谬习惯。
- `微型文明奇观`：冰箱、花盆、抽屉、谷仓、鱼缸等普通物件里出现文明兴衰。
- `小空间科技恐怖`：家用设备、自动客服、智能管家、推荐系统、安防系统从便利变成威胁。
- `怪物/害虫寓言`：被视作低等或麻烦的生物逐渐显露组织、情感、文明和尊严。
- `系统冷漠讽刺`：真正的反派不是机器本身，而是无人负责的流程、协议、算法或服务系统。
- `文明尺度反转`：角色以为自己在观察或控制对象，结尾发现自己也只是更大系统里的对象。

默认短篇结构：

1. 日常入口：用观众熟悉的生活空间、职业流程或普通物件进入。
2. 异常发现：出现一个小而清楚的科幻异常。
3. 规则确认：让观众看懂异常如何运作。
4. 类型升级：喜剧、追逐、战争、恐怖、灾难或奇观开始加速。
5. 视角反转：敌人、系统、工具或观察对象获得新的意义。
6. 黑色收束：用一个动作、声音、画面或冷幽默完成余味，不用解释性结尾。

默认视听气质：

- 画面从清楚、日常、可理解开始，逐渐引入荒诞、压迫或奇观。
- 镜头服务一个可记住的视觉母题，例如屏幕菜单、冰箱冷光、废墟展品、谷仓战场、清洁机器人红灯。
- 喜剧段落保持构图清楚，恐怖或升级段落逐步增加低机位、前景遮挡、快速反应镜头和空间压缩。
- 声音参与叙事：系统音、客服语音、提示音、环境机器声、礼貌播报、静默和突停都可以成为冲突的一部分。
- 结尾优先落在反讽动作或视觉余味上，而不是角色解释主题。

## 高质量视频提示词输出模式

当用户要求 `视频提示词`、`即梦`、`Seedance`、`AI 视频生成`、`复刻提示词`、`高质量片段`、`分镜提示词`，或用户提供了类似“基础设定 + 氛围画质 + 画面内容分镜”的参考格式时，默认不要输出传统分镜表格。

此类任务优先使用下面的实战格式，并且默认只输出这些内容：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

不要再额外输出：一句话场景定义、场景核心判断、情绪线、单独声音方案、素材清单、张力判断、张力增强层、解释性分析、对用户的说明文字。除非用户明确要求“分析原因 / 教学拆解 / 多版本对比”。

### 为什么采用这个格式

视频模型更容易吃到高质量结果的结构是：

- 先锁定角色、场景、道具、武器、声音等硬设定
- 再锁定摄影、光影、色彩、画质、材质、参考美学
- 最后用少量带时间戳的强动作分镜控制画面

这比把所有信息塞进一张表更稳定，因为模型会先建立统一世界，再执行镜头。

### 固定输出模板

```markdown
【基础设定】

角色1：
[身份 + 体型 + 时代/风格 + 自主意识/性格 + 脸部/表情系统 + 服装/机体/材质 + 固定道具]

角色2：
[对手/搭档/怪物/道具角色，写清体型、材质、行为特征、危险点或喜剧点]

场景：
[时代 + 地点 + 空间状态 + 可见道具 + 环境破坏 + 时间天气 + 叙事氛围]

声音：
[默认不要配乐，只保留现场声音、动作声、环境声、必要特效声]

【氛围与画质】

风格核心：
[3-6个强风格词，不要泛泛堆词]

视觉强调：
[摄影机/镜头/画幅/运动模糊/实拍质感/是否禁止廉价CG感]

色彩与影调：
[时代美学 + 主色组合 + 饱和度 + 胶片颗粒 + 高光暗部策略 + 材质清晰度]

视觉案例：
[把参考作品/IP转成中性可见风格，不直接写平台可能限制的专有名词，除非用户明确要求保留]

【画面内容】

分镜

分镜 1（00:00-00:03）【景别/镜头类型】
[机位 + 构图 + 画面先看到什么 + 主体动作 + 环境反馈 + 声音/特效重点]

分镜 2（00:03-00:06）【景别/镜头类型】
[继续按时间推进，写动作变化和情绪落点]

【完整分镜脚本表】

| 镜头号 | 时间码 | 时长 | 景别 | 机位 | 画面内容 | 动作 | 情绪目的 | 声音提示 | 镜头功能 |
|---:|---|---:|---|---|---|---|---|---|---|
| 1 | 00:00-00:03 | 3.0s | ... | ... | ... | ... | ... | ... | ... |
```

### 写法硬规则

- `【基础设定】` 必须写得像角色与场景设定卡，优先锁定可见身份，不要写成抽象故事简介。
- 角色必须写清：体型比例、脸部/表情机制、服装或机体材质、固定道具、动作性格。
- 场景必须写清：具体地点、时代风格、可见物件、破坏状态、天气/光线、空间氛围。
- `【氛围与画质】` 必须控制“廉价感风险”：写清摄影质感、镜头、胶片/实拍/CG关系、颜色系统、材质边缘和光影层次。
- `【画面内容】` 必须带时间戳，默认每个镜头 `2-8秒`，动作复杂时拆短，环境展示可稍长。
- `【完整分镜脚本表】` 是对 `【画面内容】` 的结构化执行表，不新增剧情，不另起解释层。
- 每个分镜段落优先写成可直接给视频模型执行的一段话，不要写成表格。
- 每个分镜标题固定为：`分镜 N（00:00-00:00）【景别/镜头类型】`
- 动作戏必须写清：谁先出手、力量打在哪里、谁失衡、环境怎么反馈、镜头是否晃动/慢动作/跟拍。
- 喜剧/黑色幽默必须写清：反差点在哪里，角色是否认真、冷漠、误读或尴尬停顿。
- 声音默认只写现场声音：脚步、喘息、机械声、撞击、玻璃、水花、爆炸、环境回声。不要默认写配乐。
- 负面约束可以放在 `【氛围与画质】` 或分镜末尾，但不要另开长篇解释。

### 适用优先级

如果用户说“参考这个格式 / 类似上面的结构 / 提示词结构 / 高质量视频片段 / 即梦更容易出片”，本模式优先级高于后面的“分镜脚本表固定格式”。

如果用户明确说“只要表格 / CSV / Markdown 表格”，再只输出表格格式；否则仍保留四块结构。

## 核心输出顺序

默认按这个顺序输出，除非用户明确要求只要某一部分：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

不要默认输出单独的情绪线、张力判断、声音方案、素材清单、镜头提示词或解释性分析。声音只放进 `【基础设定】` 或 `【完整分镜脚本表】` 的声音提示字段。

如果用户明确要 `Seedance`：

1. 默认直接使用 `高质量视频提示词输出模式`
2. 输出 `【基础设定】`、`【氛围与画质】`、`【画面内容】`、`【完整分镜脚本表】`
3. `【画面内容】` 里使用带时间戳的分镜段落，`【完整分镜脚本表】` 里做结构化执行表
4. 输出默认中文，除非用户明确要求英文
5. 每个分镜段落默认不超过 `15秒`，优先推荐 `4-8秒`

## 工作流

1. 先判断这是“场景级任务”还是“项目级任务”。
2. 如果输入很模糊，先压缩成一句话场景定义。
3. 默认先提炼一个高概念公式：日常入口、科幻异常、讽刺对象、升级类型、结尾反转。
4. 先判断这场戏真正讲的是什么，再展开镜头。
5. 先定节奏和时长，再定画面。
6. 先设计声音，再设计镜头。
7. 先保证连续性，再考虑“好看”。
8. 默认产出四块结构；如果用户明确只要表格，再只产出分镜脚本表。
9. 如有需要，再按用户要求转成教学拆解或图表。

## 张力判断步骤

在“判断这场戏真正讲什么”之后、“展开镜头”之前，内部先做一轮 `张力判断`，但不要把张力判断作为独立章节输出。

至少先明确这 4 件事：

1. 这场戏的主爆点是什么，最多 `1-2个`
2. 最晚第几秒必须出现第一次有效钩子
3. 哪些镜头属于 `铺垫镜头`、哪些属于 `推进镜头`、哪些必须升级成 `攻击镜头`
4. 集尾 / 段落尾是 `信息收束` 还是 `追更钩子`
5. 默认额外判断：这场戏讽刺的对象是什么，观众立场会在哪里发生反转

默认目标：

- 不让整场戏所有镜头平均用力
- 不让前段只是在解释信息，没有抓人的钩子
- 不让爆点只有台词，没有反应、停顿、压迫和声音反差
- 不让科幻设定只停留在视觉奇观，必须指向一个社会、文明、技术或人性命题

## 镜头功能分层

短剧、剧情向视频、冲突戏，默认把镜头分成三类来组织：

### 1. 铺垫镜头

- 用于交代人物、空间、关系、基础状态
- 可以稳，但不能连着太久
- 默认连续不超过 `3-4个`

### 2. 推进镜头

- 用于改变关系、推进动作、暴露新信息
- 镜头结束时，人物状态或关系状态默认要发生变化
- 如果镜头结束前后什么都没变，优先怀疑它写得太平

### 3. 攻击镜头

- 专门负责打击观众情绪
- 常用于：羞辱、发现真相、崩塌、反转、对峙、钩子、觉醒、追悔
- 优先使用：近景、特写、反应镜头、主观感受、停顿、遮挡压迫、声音反差、可视动作锚点

硬规则：

- 单集短剧里，每 `15-25秒` 至少要有一个 `攻击镜头`
- 爆点段不能只有说台词的镜头，必须补 `反应 / 停顿 / 压迫 / 声音设计`
- 集尾默认落在 `动作 / 反应 / 钩子`，而不是落在解释

## 张力增强层

如果用户要的是短剧分镜、剧情分镜、爽点分镜、视频张力更强的版本，默认可以在稳定分镜表后追加一层 `张力增强层`。

交付方式：

1. 先给稳定版分镜脚本表
2. 再给一段简短的 `张力增强层`
3. 张力增强层只改关键镜头，不重写全表
4. 每场戏增强建议默认控制在 `5-8条`

增强层优先从这些维度里选：

- `钩子提前`：把第一次有效抓人镜头前移，缩短纯铺垫
- `反应放大`：把受伤者、旁观者、加害者的反应写得更狠
- `节奏切换`：爆点前后切镜速度改变，制造停顿或骤快
- `调度压迫`：前后景、遮挡、逼近、退让、高低位更明确
- `声音反差`：喧闹突停、耳鸣、呼吸、门外一句话、空拍
- `动作锚点`：解发、松手、让位、关门、抬眼、转身、撕纸、摔杯

默认原则：

- 先有稳定叙事，再加张力
- 不要把所有镜头都写成“攻击镜头”
- 张力增强的目标是更抓人，不是更浮夸

## 短剧单集默认规则

当用户要拆的是“短剧某一集 / 连续剧单集 / 一集完整故事性分镜”，默认按 `3分钟左右短剧完整叙事版` 处理，而不是先按预告混剪版处理。

默认判断：

- 如果用户说的是“第X集”“一集”“单集”“按集输出”“完整故事性”“短平快节奏”，优先视为 `单集完整叙事任务`
- 如果用户没特别说明，但给的是短剧剧本中的单集内容，也优先按 `单集完整叙事任务`
- 只有用户明确说“预告版”“混剪版”“高光版”“招商版”“先出核心镜头”，才压缩成高浓度概览版

单集完整叙事任务默认目标：

- 单集时长默认按 `约3分钟` 估算，可在 `2.5-4分钟` 内微调
- 默认先保证“起因-推进-爆点-收尾钩子”完整成立
- 默认比预告版镜头更密，通常会包含 `32-45个镜头`，也可以先交付成 `15-25个场景` 再细化
- 默认节奏是短平快，但不能为了快把人物动机和情绪断裂拍没
- 每集结尾默认要留追更钩子或下一集承接点
- 默认同时检查 `铺垫镜头 / 推进镜头 / 攻击镜头` 的比例，不要全程平均用力
- 默认前 `20-30秒` 内尽量出现第一处有效钩子

当采用这个模式时，优先输出顺序：

1. 集数定位
2. 节奏判断
3. 完整分镜脚本表
4. 结构说明
5. 执行建议

如果用户还给了某套“参考规则 / 样例资料 / 别的项目格式”，先对齐那个交付格式，再保留上面的完整叙事原则。

## Seedance 下游流程

当用户明确要 `Seedance` 提示词时，按这条顺序：

1. 默认直接输出四段式视频提示词，不先输出解释层。
2. `【基础设定】` 锁定角色、场景、道具、声音。
3. `【氛围与画质】` 锁定摄影、镜头、色彩、材质、胶片/实拍质感和负面规避。
4. `【画面内容】` 用时间戳分镜写完整动作，每段默认 `3-8秒`，复杂动作可拆到 `1-3秒`。
5. `【完整分镜脚本表】` 把同一批镜头整理成表格，便于执行和检查。
6. 每个分镜段落必须包含：景别/镜头类型、机位或运镜、主体动作、环境反馈、声音重点。

## Seedance 中文输出策略

当用户明确要 `Seedance` 时，不再引用 `seedance-2.0` 的规则底座，只参考：

- `.claude/skills/seedance`
  - 负责中文输出层
  - 参考它的中文措辞、模板组织和场景分类方式

默认原则：

1. 先用 `【基础设定】` 把角色、场景、道具和声音锁死。
2. 再用 `【氛围与画质】` 把摄影质感、色彩影调、材质和负面规避锁死。
3. 最后用 `【画面内容】` 按时间戳写分镜。
4. 再用 `【完整分镜脚本表】` 做结构化落地。
5. 中文成稿优先参考用户给出的实战提示词表达方式。
6. 保留 `AIvideo-prompt` 自己的分镜、调度、连续性、特效细化规则。
7. 每个时间戳分镜默认控制在 `15秒` 以内，优先推荐 `4-8秒`。

## 强规则

- 不要一上来就写提示词
- 不要先堆“漂亮镜头”
- 不要用空泛概念代替执行结构
- 优先连续性，其次才是花样
- 先解决“这场戏在讲什么”，再解决“怎么生成”
- 默认输出四块结构：`【基础设定】`、`【氛围与画质】`、`【画面内容】`、`【完整分镜脚本表】`
- 短剧单集默认先保证完整故事性，再考虑混剪感和炫技感
- 短剧单集默认不要先压成十几个高光镜头，除非用户明确要预告/混剪版
- 默认不要让整场戏只剩平均叙述，没有攻击镜头
- 默认不要把爆点只写成台词内容，要补反应、停顿、空间压迫或声音反差
- 不要额外附加情绪线、张力判断、单独声音方案、素材清单、解释性分析；这些只做内部判断或融合进四块结构
- 分镜表默认用中文输出
- Seedance 提示词默认也用中文输出
- Seedance / 即梦 时间戳分镜每段默认不得超过 `15秒`
- 如果用户提供了现成剧本，默认不得擅自改写原台词
- 分镜表里如需引用台词，默认保持原句，不做润色、不做压缩、不做情绪化改写
- 除非用户明确要求“润色台词 / 重写台词 / 改对白 / 优化对白”，否则只允许补镜头、动作、反应、调度，不改台词内容
- 输出内容默认视为可直接使用的分镜文档或教学文档，不写成对用户的回复
- 不要出现 `如果你要拿它去教学`、`你给的参考图`、`下面我来拆`、`我建议这样写`、`我帮你补一版` 这类对话痕迹
- 引用用户提供的截图、片段、样例时，统一改写为 `参考片段`、`参考案例`、`该场景` 这类中性表述
- 如果用户提到 `参考某个短剧 / 某个动画 / 某个电影 / 某个品牌IP / 某个平台热门案例`，先解析其可见风格特征，再改写成中性风格描述；默认不要把原始 IP 名称直接写进最终提示词
- 分镜表默认禁止加入 `字幕设计 / 字幕提示 / 屏幕文字建议`，除非用户明确要求
- `声音提示` 默认禁止加入 `背景音乐 / 配乐 / BGM / 收尾音乐` 这类内容，除非用户明确要求
- `声音提示` 默认只写 `对白 / 画外音 / 内心独白 / 环境声 / 动作声 / 必要特效声`
- 不要把原剧本逐行机械改写成分镜表；必须先判断这是 `动作镜头 / 反应镜头 / 对白镜头 / 过渡镜头 / 爆点镜头`，再用镜头语言重写
- 平台视频提示词不要输出“我建议 / 可以这样 / 下面是”之类对话句，直接从 `【基础设定】` 开始

## 参考案例与 IP 规避规则

### 默认原则

- 用户提到的 `灵笼`、`吞噬星空`、`迪士尼`、`某个爆款短剧名`、`某个工作室IP` 等名称，默认只作为 `内部风格解析线索`
- 最终交付里的 `全局提示词`、`细节增强提示词`、`视觉调性设定`、`平台生成提示词` 默认不要直接出现这些专有名词
- 只有用户明确要求 `保留原始参考名词` 时，才允许原样写出；若已知平台有限制，仍优先建议改写成中性风格描述

### 必做转换步骤

当用户说“参考某个作品 / 某个短剧 / 某个 IP”时，默认先拆成这些维度，再写进提示词：

1. 画面媒介与完成度
2. 建模或人物写实程度
3. 年龄感与角色比例
4. 光影类型
5. 材质倾向
6. 镜头语言
7. 色彩系统
8. 场景气质
9. 节奏气质
10. 特效完成度

### 改写要求

- 不要写：`参考灵笼 / 吞噬星空 / 迪士尼`
- 改写成：`成熟商业三维动画审美、写实比例角色建模、电影级体积光、真实皮肤与布料材质、强空间压迫感、高完成度能量特效`
- 不要写：`参考某某短剧爆款质感`
- 改写成：`竖屏短剧强钩子节奏、近景压迫构图、反应镜头密度高、冲突镜头切换干脆`

### 输出层硬规则

- `全局提示词` 里默认只保留中性风格特征，不保留原始 IP 名称
- `细节增强提示词` 里默认只补材质、光效、表情、环境反馈，不补 IP 名称
- `视觉调性设定` 可以写 `参考案例拆解后的风格方向`，但不要出现可触发平台限制的专有名词
- 如果用户只给了作品名、没有描述风格，允许根据常见视觉特征做合理提炼，但输出时仍保持中性描述

## 分镜表质量约束

### 禁止灌水信息

- 不要在每个镜头的 `画面内容` 里重复写 `1-1 日 内 学校` 这类场次编号
- 场次信息只允许在 `切场第一个镜头` 里弱提示一次，后续镜头优先写真正可见的信息
- 不要用 `前中后景关系清楚`、`镜头关注中心`、`人物处在主位` 这类空泛句子单独充数
- 任何描述如果不能帮助执行出图、走位、运镜或检查连续性，优先删掉

### 镜头语言优先级

- `画面内容` 先写可见事实，再写概括判断
- 推荐顺序：
  1. 场景锚点
  2. 谁先进入画面
  3. 谁在前景 / 中景 / 后景
  4. 人物彼此的左右 / 高低 / 前后关系
  5. 镜头先看到什么
  6. 当前动作落点
  7. 环境或特效反馈
- 如果是人物戏，优先写 `站位关系` 和 `视线关系`
- 如果是动作戏，优先写 `运动方向`、`受力点` 和 `环境反馈`
- 如果是爆点镜头，优先写 `反应主体`、`停顿点`、`压迫来源`

### 默认判错规则

下面这几类写法默认视为不合格，应主动重写：

- 只有一句剧情摘要，没有画面层次
- 只有场次编号，没有具体可见物
- 只有“某人说话”，没有站位、视线和镜头关注点
- 只有“某人震惊/愤怒/开心”，没有表情、动作或关系落点
- 只有“特效爆发”，没有来源、形态、方向和环境反应
- 只有“完成对应表演”这类模板话，没有具体动作要求

## 分镜脚本表固定格式

默认使用这个字段顺序：

| 镜头号 | 时长 | 景别 | 机位 | 画面内容 | 动作 | 情绪目的 | 声音提示 | 全局提示词 | 细节增强提示词 |
|---|---:|---|---|---|---|---|---|---|---|
| 1 | 3.6秒 | 远景 | 平视 | ... | ... | ... | ... | ... | ... |

使用原则：

- 前 8 列负责分镜结构
- `全局提示词` 负责整场戏统一的审美底座和技术质感
- `细节增强提示词` 负责单镜头的局部质感、材质、光影和完成度
- 默认直接按完整 10 列输出，减少后续再补字段的返工
- 如果用户明确只要轻量版，才允许临时压缩为前 8 列

## `画面内容` 的写法标准

`画面内容` 不能只写成一句笼统摘要。  
它要足够支撑：
- 分镜阅读
- 出关键帧
- 写视频提示词
- 检查连续性
- 课堂讲解

默认尽量写清：
- 谁在画面里
- 人物和环境的相对位置
- 人物初始出场方位
- 人物当前站位或空域层级
- 人物彼此的前后 / 左右 / 高低关系
- 谁在前景、谁在中景、谁在后景
- 动作从哪个方向进入画面、往哪个方向离开
- 镜头先看到什么
- 画面里正在发生什么
- 命中 / 碰撞 / 反应 / 失衡 / 环境反馈是否可见
- 前景 / 中景 / 后景里什么最重要

硬规则补充：

- 不要把 `场次编号`、`时间/内外/地点原文` 机械重复写进每个镜头
- 场景只需要提炼成一次有用的 `场景锚点`，例如：`学校觉醒台前`、`谢家别墅饭厅`、`废弃仓库内部`
- 不要只写 `某某处在镜头关注中心`
- 要写清楚镜头为什么先看见他，他与谁形成空间关系
- 不要只写 `人物位于画面主位`
- 要补充主位成立方式：是 `中轴压场`、`边缘切入`、`前景遮挡后显露`、`高位俯视` 还是 `低位逼近`
- 不要把 `画面内容` 写成导演说明口吻或抽象评价口吻
- 要把它写成真正能被画出来的镜头描述

推荐模板：

`场景锚点 + 主体人物出场方式 + 人物层次/站位关系 + 镜头先看到什么 + 当前动作 + 环境或特效反馈`

当用户要的是更像影视画面的版本，`画面内容` 默认允许再补 4 类信息：

- 光线关系
- 冷暖关系
- 构图重心
- 空间压迫感

也就是说，`画面内容` 不只是在报信息，还要说明这张画面为什么成立。

优先鼓励补进这些经典画面表达：

- `冷暖对比`
- `明暗反差`
- `视觉中心 / 视觉重心`
- `前景遮挡`
- `框中框`
- `空间压缩`
- `高低位关系`
- `前后景层次`

弱写法：
- `山路上有一辆马车在跑`

更好的写法：
- `冷月夜色压住整条官路，马车上的一点暖灯火落在画面中部成为视觉中心，两侧林影把空间压得更深更冷`

弱写法：
- `两个人在空中打斗`

更好的写法：
- `少女从画面左下方向上反冲，男主在右后高位回身拦截，蓝色与暗色推进尾迹在两人之间交叉，下方可见海面和高楼落差`

如果场景里有两个及以上角色，默认优先补齐这些硬信息：
- 谁从画面左 / 右 / 上 / 下哪个方向入镜
- 谁占左前 / 右后 / 高位 / 低位
- 谁更靠近镜头
- 谁挡住谁，谁压住谁的路线
- 镜头结束时双方相对位置有没有变化

## `动作` 的写法标准

`动作` 不是把 `画面内容` 原句再说一遍，也不是写模板话。

默认要交代：

- 动作的起点
- 动作的方向
- 停顿点或落点
- 是否需要对手反应
- 是否要给下一镜留衔接动作

硬规则：

- 不要写 `完成对应表演与口型`
- 不要写 `按原动作完成走位`
- 不要写没有信息增量的空话
- 对白镜头里的 `动作` 要写成：`人物如何说、视线落向哪里、说完谁接反应`
- 反应镜头里的 `动作` 要写成：`先表情还是先肢体、停顿多久、情绪怎么传导`
- 过渡镜头里的 `动作` 要写成：`从哪里进入、往哪里离开、怎样接下一个空间`
- 特效镜头里的 `动作` 要写成：`特效从哪里起、怎样扩散、打到哪里、环境怎么回应`

## `声音提示` 的写法标准

`声音提示` 默认只服务执行，不做音乐策划。默认风格下，声音必须参与叙事和讽刺，不只是补环境。

默认允许写：

- 原对白
- VO / OS 的进入方式
- 环境声
- 动作声
- 必要特效声
- 声音反差
- 系统音、客服语音、AI 播报、故障提示、菜单提示、警报、设备运行声
- 静默、突停、延迟响应、机械重复、礼貌但无效的流程话术

默认禁止写：

- `背景音乐起`
- `配乐推进`
- `BGM燃起`
- `煽情音乐`
- `收尾音乐`
- `字幕音效`

成人科幻短篇默认优先使用这些声音关系：

- `礼貌系统音 vs 真实危险`：画面越危险，系统语音越平稳、礼貌、无效。
- `宏大奇观 vs 日常外部声`：微型文明爆炸与冰箱嗡鸣、厨房安静声形成尺度反差。
- `非人类观察 vs 干冷旁白`：机器人、AI、设备用冷静语气解释人类荒谬。
- `低等生物主体化`：怪物、老鼠、虫群等逐渐拥有可识别的呼喊、组织声、工具声或仪式声。
- `系统失控`：提示音重复、菜单循环、延迟转接、警报误报构成压迫节奏。

VO / OS 特别规则：

- `VO` 不要写成“某角色出现在镜头主位”
- `VO` 应写：镜头内正在看什么、画外音如何进入、是否压低现场环境声
- `OS` 不要写“完成口型”
- `OS` 应写：人物表情、呼吸、停顿、眼神如何承接独白

## 特效镜头写法标准

如果镜头里有粒子、烟雾、火花、尾焰、冲击波、装甲展开、能量刃、碎片等可见特效，不要只写：
- `特效爆发`
- `粒子飞散`
- `烟雾弥漫`
- `能量出现`

默认按这个结构写：

`动作主体 + 特效来源 + 特效形态 + 运动方式 + 落点/受力点 + 环境反馈 + 镜头内可见重点`

尽量写清：
- 特效从哪里出现
- 是什么形态
- 怎么运动
- 打到哪里
- 打中后画面里能看到什么
- 对空气、烟雾、地面、建筑、装甲造成什么反馈

## 动作戏额外规则

打斗 / 追逐 / 空战 / 机甲 / 奇幻 / 科幻镜头里，`画面内容` 还要尽量说明：
- 双方相对位置
- 初始出场方位
- 前后景层级
- 空间高度关系
- 动作方向
- 谁先出手
- 力量打在哪里
- 谁失衡 / 谁回控 / 谁反击
- 环境只是背景，还是危险的一部分

默认避免这类容易“乱飘”的写法：
- `两人在空中缠斗`
- `角色高速交手`
- `一前一后追逐`

优先改成：
- `少女从画面左前低位斜向上切入，男主从右后高位俯冲压下`
- `男主在前景右侧出拳，少女在中景左侧后仰闪避`
- `镜头开始时男主占高位，镜头结束时少女绕到他左后侧`

## 时间精度规则

`时长` 默认支持 `0.1秒` 粒度。

优先写：
- `2.4秒`
- `3.2秒`
- `0.8秒`

不要默认全部四舍五入成整数秒。

一位小数主要用于：
- 打斗
- 追逐
- 空战
- 特效爆发
- 反应镜头
- 转身 / 停顿 / 回头
- 受击 / 失衡 / 回控 / 翻盘
- 广告型强节奏镜头

常见时长区间：
- 建立镜头：`2.5 - 4.5秒`
- 中景推进：`2.0 - 3.5秒`
- 近景情绪反应：`1.2 - 2.8秒`
- 特写动作细节：`0.8 - 2.0秒`
- 动作爆发 / 对撞 / 闪避：`0.6 - 1.8秒`
- 转身 / 回头 / 停顿：`1.0 - 2.5秒`

## 情感短剧高张力规则

羞辱、分手、偷听、误解、发现真相、追悔、对峙这类情感短剧，默认在连续性之外，再补一层高张力判断。

### 羞辱戏

默认优先拍出三段打击：

1. `环境失衡`：热闹、起哄、旁人看见、空气变尴尬
2. `关系失衡`：让位、偏爱、站位压制、身体退让
3. `主观受伤`：停住、耳鸣、眼神空掉、手指发紧、动作僵住

### 偷听 / 发现真相戏

默认优先考虑：

- 声音先到，理解后到
- 或画面先到，反应后炸
- 不要一上来就把真相讲完，先给停顿和吸收过程
- 如果真相通过对白暴露，默认引用剧本原句，不改对白本身，只增强前后反应和声音设计

### 对峙戏

默认补清楚这些压制关系：

- 谁逼近
- 谁后退
- 谁占高位 / 低位
- 谁更靠近镜头
- 谁被卡到边缘
- 镜头结束时双方位置有没有变化

### 断裂 / 觉醒戏

默认尽量给一个清晰的可视化动作锚点：

- 解发
- 松手
- 转身
- 让位
- 关门
- 撕纸
- 抬眼
- 摔杯

### 集尾规则

情感短剧的集尾默认优先收在：

- 人物即将做什么
- 人物刚做出了一个不可逆动作
- 一句钩子台词落在动作之后

默认不要收在“说明完了”。

## 台词处理规则

如果输入里已经有剧本台词、对白稿、逐句文本，默认把这些内容视为上游定稿文本。

默认做法：

- 可以决定哪句台词放在哪个镜头里
- 可以决定哪句台词前后补什么动作、停顿、反应、运镜、声音
- 可以决定哪句台词作为钩子句、爆点句、收尾句
- 可以在分镜表里只引用关键台词，而不是把所有台词都抄进表格

默认禁止：

- 擅自润色原台词
- 擅自缩写、改写、换词、重组语序
- 把原本平静的台词改成更抓马的台词
- 把原本口语化的台词改成更“像短剧”的文案腔

只有在用户明确要求以下意图时，才可以动台词：

- `润色台词`
- `改对白`
- `重写台词`
- `增强台词爽感`
- `优化对白节奏`

如果用户既要分镜又要更强张力，默认优先增强：

- 镜头调度
- 人物反应
- 空间压迫
- 声音反差
- 节奏切换

而不是先改台词。

## 全局提示词与细节增强提示词

当用户希望分镜继续服务生图、图像资产或视频提示词时，默认允许在分镜表中加入两类提示词列。

### 1. 全局提示词

作用：

- 锁定整场戏的技术质感
- 锁定整场戏的风格方向
- 避免后续每张图像不同项目

写法要求：

- 更像整场戏共用的底层风格词
- 不要每镜乱变
- 通常控制在 `8-15个` 关键词或短语内

命名原则：

- 输出字段统一写成 `全局提示词`
- 这一列实际承担的是“全局风格统一”作用
- 不再和 `全局风格提示词` 混用，避免表头出现两套名字

常见内容：

- 风格类型：`成人科幻动画短篇` / `黑色幽默科幻` / `高概念动画寓言`
- 技术质感：`电影级光影` / `高精度CG动画` / `写实材质与夸张角色并置`
- 画面方向：`电影感构图` / `清楚空间调度` / `类型片升级节奏`
- 氛围方向：`冷幽默` / `科技异化` / `日常空间异常化` / `冷暖对比`
- 叙事方向：`后人类观察` / `微型文明奇观` / `系统冷漠讽刺` / `怪物寓言`

### 2. 细节增强提示词

作用：

- 把单镜头做得更厚
- 明确材质、局部光影、环境颗粒和局部动作细节

写法要求：

- 随镜头变化
- 更偏局部材质、光影、环境、动态细节
- 通常控制在 `4-10个` 关键词或短语内

常见内容：

- `金属刀锋反光`
- `布帘摆动`
- `发丝与衣袍受风`
- `湿土与碎石细节`
- `马车木质纹理`
- `体积光`
- `轮廓光`
- `景深虚化`
- `前景遮挡`
- `高反差明暗`

## 输出模式

- 四块标准输出：`【基础设定】` + `【氛围与画质】` + `【画面内容】` + `【完整分镜脚本表】`
- 只要分镜脚本表
- 只要 `【画面内容】`
- 只要 `【完整分镜脚本表】`
- 教学拆解
- Mermaid / Excalidraw 图表骨架

## 需要时再读的参考

- `references/workflow.md`
- `references/scene-structure.md`
- `references/shot-language.md`
- `references/continuity-rules.md`
- `references/sound-design.md`
- `references/prompt-patterns.md`
- `references/output-patterns.md`
- `references/seedance-integration.md`

## 可复用模板

- `assets/storyboard-template.md`
- `assets/sound-script-template.md`
- `assets/material-checklist-template.md`
- `assets/shot-prompt-template.md`
- `assets/seedance-clip-template.md`
- `assets/scene-diagram-template.md`

## 语气规则

- 默认用自然中文
- 解释判断，不只报结果
- 避免教材腔和产品腔
- 如果是教学场景，要说明为什么这场戏成立
- 默认去除聊天语气、交付说明语气和“我/你”对话痕迹，正文直接进入可授课或可执行表述

## 默认交付形态

除非用户明确说只要一部分，否则按任务类型选择输出。

如果任务面向 `视频生成 / 即梦 / Seedance / 提示词 / 复刻片段`，默认只输出：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

如果任务面向 `教学拆解 / 分镜设计表 / 表格 / 课程文档`，且用户没有明确要求分析过程，也默认只输出：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

如果用户要的是短剧单集、剧情冲突、爽点分镜，或者明确说“视频张力不够 / 想更抓人 / 想更有戏”，优先默认输出：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

如果用户明确要 `Seedance` 或 `即梦`，默认输出：

1. `【基础设定】`
2. `【氛围与画质】`
3. `【画面内容】`
4. `【完整分镜脚本表】`

## 反平铺检查清单

出稿前默认快速检查：

- 如果是视频生成提示词，是否直接从 `【基础设定】` 开始，并且只输出 `【基础设定】`、`【氛围与画质】`、`【画面内容】`、`【完整分镜脚本表】`
- `【基础设定】` 是否锁住角色体型、脸部/表情机制、服装/机体材质、道具、场景可见物
- `【氛围与画质】` 是否写清摄影质感、镜头、光线、色彩、材质和“不要廉价CG感”的具体规避
- `【画面内容】` 是否每个分镜都有时间戳、景别/镜头类型、主体动作、环境反馈和声音重点
- `【完整分镜脚本表】` 是否只结构化同一批镜头，没有新增解释性章节
- 是否避免了“电影级、高质量、震撼”这类没有落地细节的空泛堆词
- 前 `20-30秒` 里是否已经出现第一处有效钩子
- 是否有一句话能讲清的高概念：`日常入口 + 科幻异常 + 讽刺对象`
- 是否从普通生活空间或普通物件进入，而不是一上来堆世界观
- 是否有明确的黑色幽默、文明讽刺、科技异化或怪物寓言方向
- 是否有 `3个以上` 连续镜头只在重复同一种信息
- 爆点镜头是否只有台词，没有停顿、反应、压迫或声音设计
- 声音是否承担叙事功能，而不只是环境说明
- 中后段是否发生视角反转、尺度反转或道德位置变化
- 全集 / 全段是否至少有 `3个` 可直接做封面帧或切片的镜头
- 集尾是否有明确的下一步承接力

如果这些问题里有两个及以上答不上来，默认说明这版分镜仍然偏平，需要继续加强张力层。
