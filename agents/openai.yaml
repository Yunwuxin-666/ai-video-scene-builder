interface:
  display_name: "AIVideoPro"
  short_description: "Turn an AI video idea into a production-ready storyboard package."
  default_prompt: "Use $AIVideoPro to turn this video idea or scene into a production-ready storyboard package."

policy:
  allow_implicit_invocation: true
